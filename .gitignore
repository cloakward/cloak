target/
**/*.rs.bk
Cargo.lock.bak
.cargo/libsodium-dist/

node_modules/
dist/
.bun/
# Bun's transient compile-output files (~57 MB each).
*.bun-build
.*.bun-build

# Local vault and audit artifacts (must NEVER be committed)
*.cloak
*.cloak-bundle
audit.jsonl
.cloak-state/
# Plaintext dotenv files - the very thing Cloak exists to replace.
.env
.env.*

# OS / IDE
.DS_Store
.vscode/
.idea/
*.swp

# Internal planning / process docs - local only, not for the public repo.
PLAN.md
PLAN_V1.md
compass_artifact_*.md
/docs/internal/
